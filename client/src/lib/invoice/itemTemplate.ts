/**
 * The invoice item table uses a FIXED number of rows (per product decision —
 * not a dynamic add/remove list). Change ROW_COUNT or the default labels
 * here and every form/view picks it up automatically.
 */
export const INVOICE_ITEM_ROW_COUNT = 1;

export const DEFAULT_ITEM_LABELS: string[] = ['Cash', '', ''];
